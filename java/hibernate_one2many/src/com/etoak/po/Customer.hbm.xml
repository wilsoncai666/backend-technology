<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">
<!-- 
    Mapping file autogenerated by MyEclipse Persistence Tools
-->
<hibernate-mapping>
    <class name="com.etoak.po.Customer" table="customer" catalog="etoak">
        <id name="id" type="java.lang.Integer">
            <column name="id" />
            <generator class="identity" />
        </id>
        <property name="name" type="java.lang.String">
            <column name="name" length="32" />
        </property>
        <!-- inverse:表示关联字段的维护权是否反转
        	【是否交给对方管理生成关联在字段的值】
        		false:不反转 不交给对方 默认
        		insert into customer values(xx,xxxx);
        		insert into orders values(xxx,xxx,null);
        		insert into orders values(xxx,xxx,null);
        		update orders set cid = cus.id where id=?
        		update orders set cid = xxx where id=?
        		true:反转 交给对方
        		insert into customer values(xx,xxxx);
        		insert into orders values(xxx,xxx,order.getCus().getId());
        		insert into orders values(xxx,xxx,null);
         -->
        <set name="orders" lazy="false" inverse="true" cascade="all">
        	<!-- 关联字段 -->
        	<key column="cid"></key>
        	<one-to-many class="com.etoak.po.Orders"/>
        </set>
    </class>
</hibernate-mapping>
